package com.gestion.permisos.repositorio;


import com.gestion.permisos.entidades.SolCometido;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SolCometidoRepository extends JpaRepository<SolCometido, Long> {
}
