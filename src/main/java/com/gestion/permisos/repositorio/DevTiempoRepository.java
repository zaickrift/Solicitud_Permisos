package com.gestion.permisos.repositorio;

import com.gestion.permisos.entidades.DevTiempo;

import org.springframework.data.jpa.repository.JpaRepository;

public interface DevTiempoRepository extends JpaRepository<DevTiempo, Long> {
}
